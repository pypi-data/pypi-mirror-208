[tool.poetry]
name = "click-aliasing"
version = "1.0.0"
description = "Enable aliasing and unique matching for Click"
authors = ["tjzegmott <tzegmott@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "click_aliasing"}]

[tool.poetry.dependencies]
python = ">=3.8,<4.0"
click = "^8.1.3"

[tool.poetry.group.dev.dependencies]
pytest = "^7.3.1"
pytest-cov = "^4.0.0"
pre-commit = "^3.3.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
