[tool.poetry]
name = "biocolabsdk"
version = "1.0.4"
description = "A set of python modules for accessing BioTuring Ecosystem on BioColab private server"
authors = ["BioTuring <support@bioturing.com>"]
readme = "README.md"
packages = [{include = "biocolabsdk"}]

[tool.poetry.dependencies]
python = "^3.7"
bioturing-connector = "^1.1"
bioflex = "^1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
