Metadata-Version: 2.1
Name: jamespopcat
Version: 1.0.2
Summary: Auto clicker Popcat
Author: CTRCRK
Author-email: ctrcrk.id@gmail.com
Description-Content-Type: text/markdown
Requires-Dist: selenium
Requires-Dist: psutil

## Jamespopcat Python Library

Jamespopcat is a program designed to automate clicks on the website [popcat.click](https://popcat.click/).

### Requirements

Before installing the Jamespopcat program, make sure that your computer meets the following requirements:

- Python 3.6 or newer is installed on your computer.
- Google Chrome is installed on your computer.
- You have downloaded the `chromedriver.exe` file that matches the version of Google Chrome installed on your computer and placed it in the same directory as the program file. (https://chromedriver.chromium.org/)

### Installation 

1. Open the terminal or command prompt on your computer.

2. Install the Jamespopcat package by running the following command:

   ```
   pip install jamespopcat
   ```
   
3. To ensure that you have installed the latest version, use the following command:

   ```
   pip install --upgrade jamespopcat
   ```
   
4. To run the program, run the following command in the terminal or command prompt:

   ```
   python -m jamespopcat
   ```

### Notes

- This program can only be run on desktop operating systems such as Windows, MacOS, and Linux.
- Make sure you have downloaded the `chromedriver.exe` file that matches the version of Google Chrome installed on your computer and placed it in the same directory as the program file.
- The program will consume significant CPU resources because it continuously clicks on the popcat.click website.
