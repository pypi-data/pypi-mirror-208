[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kommons"
version = "0.0.1"
description = ""
requires-python = ">=3.7"
authors = [{ name = "Ralph Urlus", email = "rurlus.dev@gmail.com" }]
dependencies = []
