[build-system]
requires = ["poetry>=1.1.0"]
build-backend = "poetry.masonry.api"

[tool.poetry]
name = "t4flib"
version = "0.1.0"
description = "Une librairie python pour Tech4Finance"
authors = ["cdevrouete@partner.auchan.fr"]

[tool.poetry.dependencies]
python = "^3.8"
# Ajoutez les dépendances supplémentaires requises par votre projet ici

[tool.poetry.dev-dependencies]
# Ajoutez ici les dépendances de développement nécessaires pour votre projet, par exemple pour les tests unitaires

[build-system]
requires = ["poetry>=1.1.0"]
build-backend = "poetry.masonry.api"
