[build-system]
requires = ["setuptools>=61.0.0","wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kagglebh"
version = "1.0.0"
description = "Analyze kaggle dataset: climate-change-earth-surface-temperature"
readme = "README.md"
authors = [{ name = "bart", email = "henzel.b@gmail.com" }]

keywords = ["test", "task"]
dependencies = ["kaggle",
"pyspark"
]

requires-python = ">=3.8"

[project.optional-dependencies]
dev = []

[project.urls]
Homepage = "https://github.com/HenzelBartlomiej/kaggle_datasets"