[project]
name = "disnake-ext-plugins"
version = "0.0.0-alpha.0"
description = "An extension for disnake providing a robust alternative to cogs, not reliant on inheritance."
authors = [
    { name = "Chromosomologist", email = "chromosomologist@gmail.com" },
]
dependencies = []
requires-python = ">=3.8"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool]
