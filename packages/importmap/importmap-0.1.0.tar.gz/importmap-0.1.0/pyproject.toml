[tool.poetry]
name = "importmap"
version = "0.1.0"
description = "JavaScript import maps support for framework agnostic static files."
authors = ["Josiah Kaviani <proofit404@gmail.com>"]
license = "BSD-2-Clause"
readme = "README.md"

[tool.poetry.dependencies]
python = "*"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
