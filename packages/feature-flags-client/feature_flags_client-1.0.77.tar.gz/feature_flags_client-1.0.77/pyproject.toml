[tool.poetry]
name = "feature-flags-client"
version = "1.0.77"
description = ""
authors = ["Stitch"]
readme = "README.md"
packages = [{include = "flags_be_client"}]

[tool.poetry.dependencies]
python = "^3.8"
requests = "2.23.0"
crc32c = "^2.3.post0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.3.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
